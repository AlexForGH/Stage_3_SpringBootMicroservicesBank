spring:
  application:
    name: bank-account-service

  kafka:
    producer:
      bootstrap-servers: ${KAFKA_SERVER_URL:localhost:9092}

  config:
    import: configserver:${GLOBAL_CONFIG_SERVER_URL:http://localhost:10000} # Параметры подключения к Cloud Config Server